<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login | TicketBook</title>
    <link rel="stylesheet" href="/css/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

    <div class="netflix-navbar">
        <div class="logo">ticketBook</div>
    </div>

    <div class="auth-wrapper">
        <div class="auth-box">
            <h1 class="auth-title">Sign In</h1>
            
            <% if (locals.error) { %>
                <div class="error-msg"><%= error %></div>
            <% } %>

            <form action="/login" method="POST">
                <div class="form-group">
                    <input type="email" name="email" class="form-input" placeholder="Email or phone number" required>
                </div>
                <div class="form-group">
                    <input type="password" name="password" class="form-input" placeholder="Password" required>
                </div>
                
                <button type="submit" class="btn-submit">Sign In</button>
                
                <div style="display: flex; justify-content: space-between; font-size: 13px; color: #b3b3b3;">
                    <label><input type="checkbox" checked> Remember me</label>
                    <a href="#" style="color: #b3b3b3;">Need help?</a>
                </div>
            </form>

            <div class="auth-footer">
                New to TicketBook? <a href="/register">Sign up now</a>.
                <br><br>
                <span style="font-size: 13px;">This page is protected by Google reCAPTCHA to ensure you're not a bot.</span>
            </div>
        </div>
    </div>

</body>
</html>