<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>My Tickets</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .ticket-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }
        .ticket-card {
            background: #181818;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.2s;
            border: 1px solid #333;
        }
        .ticket-card:hover {
            transform: translateY(-5px);
            border-color: #555;
        }
        .ticket-header {
            background: #E50914;
            color: white;
            padding: 15px;
            font-weight: bold;
            font-size: 1.1rem;
        }
        .ticket-body {
            padding: 20px;
            color: #ddd;
        }
        .ticket-info {
            margin-bottom: 10px;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
        }
        .ticket-info i {
            width: 25px;
            color: #E50914;
        }
        .ticket-footer {
            background: #222;
            padding: 10px 20px;
            font-size: 0.8rem;
            color: #888;
            text-align: right;
        }
    </style>
</head>
<body>

    <div class="netflix-navbar">
        <div class="logo">ticketBook</div>
        <div class="nav-right">
             <a href="/home" style="color: #fff; font-weight: bold;">Home</a>
             <a href="/logout" class="btn-logout">Logout</a>
        </div>
    </div>

    <div class="container" style="padding-top: 100px; max-width: 1200px; margin: 0 auto; padding-left: 20px; padding-right: 20px;">
        <h1 style="border-left: 5px solid #E50914; padding-left: 15px;">My Tickets</h1>

        <% if (tickets && tickets.length > 0) { %>
            <div class="ticket-grid">
                <% tickets.forEach(ticket => { %>
                    <div class="ticket-card">
                        <div class="ticket-header">
                            <%= ticket.movie?.title || "Unknown Movie" %>
                        </div>
                        <div class="ticket-body">
                            <div class="ticket-info">
                                <i class="fas fa-map-marker-alt"></i> <%= ticket.city %>
                            </div>
                            <div class="ticket-info">
                                <i class="far fa-clock"></i> <%= ticket.show?.time || "N/A" %>
                            </div>
                            <div class="ticket-info">
                                <i class="fas fa-tv"></i> <%= ticket.show?.screen?.name || "Screen 1" %>
                            </div>
                            <div class="ticket-info">
                                <i class="fas fa-couch"></i> <%= ticket.seats.join(", ") %>
                            </div>
                        </div>
                        <div class="ticket-footer">
                            Booked on: <%= ticket.createdAt.toDateString() %>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div style="margin-top: 50px; text-align: center; color: #aaa;">
                <i class="fas fa-ticket-alt" style="font-size: 50px; margin-bottom: 20px; color: #333;"></i>
                <h3>No tickets booked yet.</h3>
                <p>Go to the <a href="/home" style="color: #E50914; text-decoration: underline;">Home Page</a> to book your first movie.</p>
            </div>
        <% } %>
    </div>

</body>
</html>
